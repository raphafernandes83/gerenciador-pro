# Roadmap de Execução - 28/11/25 23:30

## 🛡️ Fase 1: Blindagem de Segurança (Prioridade Máxima)
- [ ] **1.1 Correção de Path Traversal no Server**
    - [ ] Validar se o path resolvido está dentro do `__dirname`.
    - [ ] Bloquear tentativas de acesso a arquivos ocultos ou sensíveis.
- [ ] **1.2 Otimização de I/O (Anti-DoS)**
    - [ ] Substituir `fs.readFile` por `fs.createReadStream`.
    - [ ] Implementar tratamento de erro no stream (pipe).
- [ ] **1.3 Headers de Segurança**
    - [ ] Adicionar headers `X-Content-Type-Options`, `X-Frame-Options`, `Cache-Control`.

## 🧹 Fase 2: Limpeza e Higiene (Housekeeping)
- [ ] **2.1 Remoção de Arquivos Mortos**
    - [ ] Deletar `server-nocache.js`.
    - [ ] Deletar `temp_from_server.html` e `temp_style.css`.
    - [ ] Mover ou deletar backups antigos da raiz (`*.backup*`).

## 🔧 Fase 3: Melhorias no Backup (Client-Side)
- [ ] **3.1 Otimização de Performance**
    - [ ] Substituir `btoa` (Base64) por compressão real (GZIP/Deflate) se possível, ou otimizar serialização.
    - [ ] Garantir que o backup não bloqueie a UI (usar `requestIdleCallback` ou `setTimeout` para fatiar o trabalho se necessário).

## 🧪 Fase 4: Validação Final
- [ ] Testar servidor com requisições maliciosas (`../`).
- [ ] Verificar consumo de memória com arquivos grandes.
- [ ] Confirmar que a aplicação continua funcional após limpeza.
