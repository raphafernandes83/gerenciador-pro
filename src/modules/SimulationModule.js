/**
 * Módulo de Simulação (Stub)
 */
export const simulation = {
    runMonteCarlo(params) {
        console.log('Executando simulação Monte Carlo (Stub) com:', params);
        return {
            successRate: 0.5,
            ruinRate: 0.5,
            averageProfit: 0,
            medianProfit: 0
        };
    }
};
